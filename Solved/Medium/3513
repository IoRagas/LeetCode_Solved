#The maximum number you can get will be your ( MSB turned on, and other less than it too)
#So ans > ( 2 ^ msb_pos ) , and see zero can be generated only with 0 , 1 , 2
# You gonna need 1 , 2 , 3 to generate zero ..... THATS ALL IG.... IF You're Reading this...
# You got better things to do buddy.
class Solution:
    def uniqueXorTriplets(self, nums: List[int]) -> int:
        ans = 0
        n = len(nums)
        if n == 1 : 
            return 1
        if n == 2 :
            return 2
        pos = 0
        temp = n
        while temp > 0 : 
            pos += 1
            temp = temp >> 1
        print(pos)
        return ( 2 ** pos ) 
         
